<?xml version="1.0" encoding="utf-8"?>
<!--
  资源文件：item_product_image_picker.xml
  项目职责：item_product_image_picker.xml Android 资源文件，负责项目中对应界面或系统配置。
  配合代码：Java 代码通过 setContentView、inflate、findViewById 和 R.id/R.layout 引用这里的控件。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="100dp"
    android:layout_height="100dp"
    android:layout_margin="4dp">

    <ImageView
        android:id="@+id/ivPickerImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:background="#EDEDEF" />

    <ImageView
        android:id="@+id/ivDeleteImage"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_gravity="top|end"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:background="@drawable/bg_glass_pill"
        android:padding="4dp"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/llAddPlaceholder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="#F5F5F5"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+"
            android:textSize="32sp"
            android:textColor="#AAAAAA" />
    </LinearLayout>

</FrameLayout>
